[pytest]
# Disable the langsmith pytest plugin — it crashes on startup due to a
# pydantic-core version mismatch in the system Python environment.
# This is unrelated to FinPulse code; it is a system-level package conflict.
addopts = --import-mode=importlib
